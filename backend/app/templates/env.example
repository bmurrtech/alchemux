# =============================================================================
# Alchemux .env (secrets & sensitive runtime config only)
# =============================================================================
# This file is a template. Copy to .env and fill in your values.
# The .env file is gitignored and stored securely (chmod 600).
#
# IMPORTANT: Non-secret configuration (user preferences, defaults, etc.)
# should be set in config.toml, not here. This file is ONLY for secrets.
#
# To configure non-secret settings:
#   - Edit config.toml directly, or
#   - Use: alchemux config set <key> <value>
#
# See config.toml for all non-secret configuration options.
# =============================================================================

# =============================================================================
# OAuth (treat as sensitive; secrets go here)
# =============================================================================
# OAuth cache directory (non-secret) can be set in config.toml if needed.
# Client ID and secret are sensitive and must go here.
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=

# =============================================================================
# GCP secrets
# =============================================================================
# Bucket name can be set in config.toml (storage.gcp.bucket);
# service account key is secret and must go here.
GCP_SA_KEY_BASE64=

# =============================================================================
# S3 secrets
# =============================================================================
# Endpoint and bucket can be set in config.toml (storage.s3.endpoint, storage.s3.bucket);
# access and secret keys are secrets and must go here.
S3_ACCESS_KEY=
S3_SECRET_KEY=
