# prek (pre-commit replacement) — run hooks on push and PR
# See https://github.com/j178/prek-action
name: Prek checks
on: [push, pull_request]
jobs:
  prek:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: j178/prek-action@v1
