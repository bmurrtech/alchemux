# =============================================================================
# Alchemux - Transmute URLs to usable media formats.
# =============================================================================
# This file is a template. Copy to .env and fill in your values.
# The .env file is gitignored and stored securely (chmod 600).

# =============================================================================
# EULA Acceptance
# =============================================================================
# Set to 'true' to accept the EULA (or use --accept-eula flag)
# EULA_ACCEPTED=false
# EULA_ACCEPTED_AT=
# EULA_ACCEPTANCE_HASH=

# =============================================================================
# Local Download Configuration
# =============================================================================
# Default download path (relative to executable or absolute path)
# Can be overridden with --save-path flag (which updates this value)
DOWNLOAD_PATH=./downloads

# Temporary directory for processing (defaults to system temp if not set)
# TEMP_PATH=./temp

# =============================================================================
# Audio Conversion Settings
# =============================================================================
# Audio format (default: mp3)
# Supported formats: mp3, aac, alac, flac, m4a, opus, vorbis, wav
# Default: mp3 (most compatible)
# Can be overridden with --audio-format flag
# AUDIO_FORMAT=mp3

# MP3 audio quality (VBR 0-10, where 5 is default)
# Only applies to MP3 format
# 0 = lowest quality, smallest file
# 10 = highest quality, largest file
# AUDIO_QUALITY=5

# Audio sample rate (optional, for specific formats)
# Examples: 16000 (16kHz), 44100 (CD quality), 48000 (standard)
# If not set, uses source sample rate
# AUDIO_SAMPLE_RATE=

# Audio channels (optional, for specific formats)
# Examples: 1 (mono), 2 (stereo)
# If not set, uses source channel count
# AUDIO_CHANNELS=

# FLAC override (optional)
# If set to 'true', automatically applies 16kHz mono settings to FLAC conversion
# Equivalent to --flac flag behavior (sample rate: 16000, channels: mono)
# If false or unset, FLAC uses source audio properties unless --flac flag is used
# FLAC_OVERRIDE=true

# =============================================================================
# Video Conversion Settings (Optional - MVP focuses on audio)
# =============================================================================
# Video format/container (default: mp4)
# Supported formats: mp4, mkv, webm, mov, avi, flv, gif
# Default: mp4 (most compatible)
# Can be overridden with --video-format flag
# Note: Remuxing (container change) is fast; re-encoding is resource-intensive
# yt-dlp uses remuxing when codec is compatible, re-encoding only if necessary
# VIDEO_FORMAT=mp4

# Video codec preference (optional)
# Supported codecs: h264, h265, vp9, vp9.2, av01, vp8, h263, theora
# Default: h264 (most compatible)
# VIDEO_CODEC=h264

# =============================================================================
# yt-dlp Configuration
# =============================================================================
# Restrict filenames for cross-platform compatibility (default: true)
# RESTRICT_FILENAMES=true

# Overwrite existing files (default: false, uses --no-overwrites)
# FORCE_OVERWRITES=false

# Number of retries for failed downloads (default: 10)
# RETRIES=10

# =============================================================================
# Authentication & Cookies
# =============================================================================
# YouTube OAuth (optional, for age-restricted content)
# OAUTH_CLIENT_ID=your-oauth-client-id
# OAUTH_CLIENT_SECRET=your-oauth-client-secret
# OAUTH_CACHE_DIR=./oauth_cache

# Facebook cookies (optional, for private/authenticated content)
# FACEBOOK_COOKIES_FILE=./cookies.txt
# FACEBOOK_COOKIES_BASE64=base64_encoded_cookies_file

# Browser cookies (optional, extract from logged-in browser)
# BROWSER_COOKIES_BROWSER=chrome
# BROWSER_COOKIES_PROFILE=Default

# =============================================================================
# Cloud Storage Configuration (Optional)
# =============================================================================

# PRIMARY: Google Cloud Storage
# Required if using --gcp flag
# GCP_STORAGE_BUCKET=your-gcp-storage-bucket-name
# GCP_SA_KEY_BASE64=your_base64_encoded_service_account_key

# S3-Compatible Storage (Optional, for future --s3 flag)
# S3_ENDPOINT=https://your-minio.cloudron.app
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key
# S3_BUCKET=your-bucket-name
# S3_SSL=true

# =============================================================================
# Logging & Debugging
# =============================================================================
# Log level: 'info' (default) or 'debug' (verbose)
LOG_LEVEL=info

# Enable verbose yt-dlp output (default: false)
# VERBOSE=false

# =============================================================================
# User Experience
# =============================================================================
# Auto-open folder: Default is 'true' (automatically opens folder after download).
# Uncomment and set to 'false' to disable auto-open behavior.
# AUTO_OPEN=false

# Arcane terminology: Default is 'true' (uses arcane-themed terminology like
# distill, mux, seal, etc.). Uncomment and set to 'false' to use technical terms
# (download, embed, complete, etc.). See docs/legend.md for full terminology mapping.
# ARCANE_TERMS=false
